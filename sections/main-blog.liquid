

<script
  src="{{ 'blog-posts-list.js' | asset_url }}"
  type="module"
  fetchpriority="low"
></script>

<blog-posts-list section-id="{{ section.id }}" pagination-type="{{ section.settings.pagination_type }}">
  <div class="section-background color-{{ section.settings.color_scheme }}"></div>
  <div
    class="
      section
      color-{{ section.settings.color_scheme }}
      blog-posts
      spacing-style
      size-style
    "
    data-testid="blog-posts"
    style="{% render 'spacing-style', settings: section.settings %}"
  >
    {% content_for 'blocks' %}

    {% liquid
      assign posts_per_page = section.settings.posts_per_page
      assign show_hero = section.settings.show_hero_post
      assign columns = section.settings.columns | plus: 0
      assign columns_mobile = section.settings.columns_mobile | default: 1 | plus: 0
    %}

    {% paginate blog.articles by posts_per_page %}
      <div
        ref="grid"
        class="blog-posts-container"
        data-last-page="{{ paginate.pages }}"
        style="--columns: {{ columns }}; --columns-mobile: {{ columns_mobile }}; --columns-gap: {{ section.settings.columns_gap }}px; --rows-gap: {{ section.settings.rows_gap }}px;"
      >
        {% for article in blog.articles %}
          {% liquid
            assign is_hero = false

            if show_hero and forloop.first
              assign is_hero = true
            endif
          %}
          <div
            ref="cards[]"
            class="blog-post-item{% if is_hero %} blog-post-item--horizontal{% endif %}"
            data-testid="blog-post-item"
            data-page="{{ paginate.current_page }}"
          >
            {% content_for 'block', id: 'static-blog-post-card', type: '_blog-post-card', article: article %}
          </div>
        {% endfor %}
      </div>

      {% if paginate.next %}
        {% if section.settings.pagination_type == 'infinite_scroll' %}
          <div ref="viewMoreNext" class="blog-posts-scroll-sentinel" aria-hidden="true"></div>
        {% elsif section.settings.view_more_text != blank %}
          <div class="blog-posts-view-more">
            <a ref="viewMoreNext" href="{{ paginate.next.url }}" class="{{ section.settings.view_more_style }}">
              {{ section.settings.view_more_text }}
            </a>
          </div>
        {% endif %}
      {% endif %}
    {% endpaginate %}
  </div>
</blog-posts-list>

{% stylesheet %}
  /**
   * Blog posts page layout
   */
  .blog-posts {
    --page-content-width: var(--narrow-page-width);
    --page-width: calc(var(--page-content-width) + (var(--page-margin) * 2));
  }

  .blog-posts-container {
    display: grid;
    grid-template-columns: repeat(var(--columns), 1fr);
    width: 100%;
    column-gap: var(--columns-gap);
    row-gap: var(--rows-gap);
  }

  @media screen and (max-width: 749px) {
    .blog-posts-container {
      grid-template-columns: repeat(var(--columns-mobile), 1fr);
    }
  }

  .blog-post-item--horizontal {
    grid-column: 1 / -1;
  }

  /**
   * When there's no image, the blog post item has a border.
   */
  .blog-post-item {
    border: 1px solid rgb(var(--color-foreground-rgb) / var(--opacity-20));
    padding: 0 1rem 1rem;
  }

  .blog-post-item:has(.blog-post-card__image-container) {
    border: none;
    padding: 0;
  }

  .blog-posts-scroll-sentinel {
    height: 1px;
  }

  .blog-posts-view-more {
    display: flex;
    justify-content: center;
    margin-top: 36px;
  }

  .blog-posts-view-more .link {
    display: inline-block;
    text-decoration: none;
    font-size: var(--font-size-static-small);
    letter-spacing: 0.04em;
    border-bottom: 1px solid currentColor;
    padding-bottom: 0.15em;
    transition: opacity 0.2s;
  }

  .blog-posts-view-more .link:hover {
    opacity: 0.7;
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:names.blog_posts",
  "blocks": [
    {
      "type": "@theme"
    },
    {
      "type": "@app"
    },
    {
      "type": "text"
    },
    {
      "type": "icon"
    },
    {
      "type": "image"
    },
    {
      "type": "button"
    },
    {
      "type": "video"
    },
    {
      "type": "group"
    },
    {
      "type": "spacer"
    },
    {
      "type": "_divider"
    }
  ],
  "disabled_on": {
    "groups": ["header"]
  },
  "settings": [
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "select",
      "id": "columns",
      "label": "Columns",
      "options": [
        {
          "value": "2",
          "label": "2"
        },
        {
          "value": "3",
          "label": "3"
        },
        {
          "value": "4",
          "label": "4"
        }
      ],
      "default": "4"
    },
    {
      "type": "select",
      "id": "columns_mobile",
      "label": "Mobile columns",
      "options": [
        {
          "value": "1",
          "label": "1"
        },
        {
          "value": "2",
          "label": "2"
        }
      ],
      "default": "1"
    },
    {
      "type": "checkbox",
      "id": "show_hero_post",
      "label": "Show featured post as hero",
      "info": "Display the first post full-width at the top",
      "default": false
    },
    {
      "type": "header",
      "content": "Pagination"
    },
    {
      "type": "range",
      "id": "posts_per_page",
      "label": "Posts per page",
      "min": 3,
      "max": 12,
      "step": 3,
      "default": 12
    },
    {
      "type": "select",
      "id": "pagination_type",
      "label": "Pagination style",
      "options": [
        {
          "value": "infinite_scroll",
          "label": "Infinite scroll"
        },
        {
          "value": "button",
          "label": "Button"
        }
      ],
      "default": "infinite_scroll"
    },
    {
      "type": "text",
      "id": "view_more_text",
      "label": "Button label",
      "default": "View more",
      "visible_if": "{{ section.settings.pagination_type == 'button' }}"
    },
    {
      "type": "select",
      "id": "view_more_style",
      "label": "Button style",
      "visible_if": "{{ section.settings.pagination_type == 'button' }}",
      "options": [
        {
          "value": "button",
          "label": "t:options.primary"
        },
        {
          "value": "button-secondary",
          "label": "t:options.secondary"
        },
        {
          "value": "link",
          "label": "t:options.link"
        }
      ],
      "default": "button"
    },
    {
      "type": "header",
      "content": "Spacing"
    },
    {
      "type": "range",
      "id": "columns_gap",
      "label": "Column gap",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 36
    },
    {
      "type": "range",
      "id": "rows_gap",
      "label": "Row gap",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 36
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:settings.color_scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:content.padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "t:settings.top",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "t:settings.bottom",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 0
    }
  ]
}
{% endschema %}
